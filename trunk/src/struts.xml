<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
	<constant name="struts.devMode" value="false" /> <!-- set to false for prod -->
	<!-- 
    <package name="movimiento" namespace="/movimiento" extends="struts-default">
		<action name="*" class="actions.movimiento.{1}">
			<result>/views/movimiento/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>

   <package name="lista" namespace="/lista" extends="struts-default">
		<action name="*" class="actions.lista.{1}">
			<result>/views/lista/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>	
	
    <package name="rubro" namespace="/rubro" extends="struts-default">
		<action name="*" class="actions.rubro.{1}">
			<result>/views/rubro/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>
	
	<package name="generar" namespace="/generar" extends="struts-default">
		<action name="*" class="actions.generar.{1}">
			<result>/views/generar/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>
	
    <package name="afiliado" namespace="/afiliado" extends="struts-default">
		<action name="*" class="actions.afiliado.{1}">
			<result>/views/afiliado/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>
	
	<package name="ubicacion" namespace="/ubicacion" extends="struts-default">
		<action name="*" class="actions.ubicacion.{1}">
			<result>/views/ubicacion/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>
	
	
	<package name="convenio" namespace="/convenio" extends="struts-default">
		<action name="*" class="actions.convenio.{1}">
			<result>/views/convenio/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>
	-->
	
	<package name="root-redirect" namespace="/" extends="struts-default">
		<action name="*" class="actions.usuarios.Login" >
			<result type="redirect">usuarios/Login.action</result>
		</action>
	</package>
	
	<package name="usuarios" namespace="/usuarios" extends="struts-default">
		<action name="*" class="actions.usuarios.{1}">
			<result>/views/usuarios/{1}.jsp</result>
			<result name="redirect" type="redirect">${redirectUrl}</result>
		</action>
	</package>
	 

</struts>
